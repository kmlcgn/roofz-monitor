[build]
builder = "nixpacks"

[build.nixpacksConfigPath]
configPath = "nixpacks.toml"

[deploy]
startCommand = "playwright install chromium && playwright install-deps && python monitor.py"
restartPolicyType = "always"
